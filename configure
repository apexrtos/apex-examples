#!/bin/sh

quit()
{
	cat >&2 <<ERRORMSG

ERROR: $@
ERRORMSG
	exit 1
}

a="/$0"; a=${a%/*}; a=${a:-.}; a=${a#/}/; srcdir=$(cd "$a" && pwd)
builddir=$(pwd)

[ "$srcdir" = "$builddir" ] &&
	quit "in-tree build not supported"

"$srcdir/apex/configure" --srcdir=.. "$@"
